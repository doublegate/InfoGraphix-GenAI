# InfoGraphix AI - Environment Variables Example
#
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to version control

# =============================================================================
# API Mode Configuration (v2.2.0+)
# =============================================================================

# API Mode: Controls which backend is used for infographic generation
# Options:
#   - 'gemini'     : Direct Gemini API calls (default, current behavior)
#   - 'infographix': InfoGraphix AI API (Q2 2026, uses mock for now)
#   - 'auto'       : Automatically selects based on available keys
VITE_API_MODE=gemini

# =============================================================================
# Google Gemini API (Default Mode)
# =============================================================================

# Google Gemini API Key
# Required for 'gemini' mode and Google AI Studio deployment
# Get your key from: https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Legacy alias (also supported)
GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# InfoGraphix AI API (Future - Q2 2026)
# =============================================================================

# InfoGraphix API Key
# Required for 'infographix' mode
# Get your key from: https://infographix.ai/dashboard/api-keys (coming Q2 2026)
# VITE_INFOGRAPHIX_API_KEY=your_infographix_api_key_here

# InfoGraphix API Base URL (optional)
# Default: https://api.infographix.ai/v1
# Use for custom deployments or development servers
# VITE_INFOGRAPHIX_BASE_URL=https://api.infographix.ai/v1

# =============================================================================
# Development Options
# =============================================================================

# Enable debug logging for API service
# VITE_DEBUG=true

# Mock client options (InfoGraphix mode only)
# Simulate analysis delay in milliseconds (default: 2000)
# VITE_MOCK_ANALYSIS_DELAY=2000

# Simulate generation delay in milliseconds (default: 3000)
# VITE_MOCK_GENERATION_DELAY=3000

# Simulate error rate (0-1, default: 0 = no errors)
# VITE_MOCK_ERROR_RATE=0

# =============================================================================
# Notes
# =============================================================================
#
# The VITE_ prefix is required for environment variables to be exposed
# to the browser in Vite-based applications.
#
# For CI/CD:
#   - Set GEMINI_API_KEY in your CI secrets
#   - Set CODECOV_TOKEN for test coverage uploads
#
# For production deployment:
#   - In Google AI Studio: API keys are selected via the AI Studio interface
#   - For InfoGraphix API: Set VITE_INFOGRAPHIX_API_KEY and VITE_API_MODE=infographix
