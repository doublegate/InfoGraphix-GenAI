# InfoGraphix AI - Environment Variables Example
#
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to version control

# Google Gemini API Key (Optional for local development)
# Required for local development. In Google AI Studio deployment, API keys are
# selected via the AI Studio interface.
# Get your key from: https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Note: The VITE_ prefix is required for environment variables to be exposed
# to the browser in Vite-based applications.
