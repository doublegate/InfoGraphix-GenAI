name: Dependency Review

on:
  pull_request:
    branches: [main]
    paths:
      - 'package.json'
      - 'package-lock.json'

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: deps-${{ github.ref }}
  cancel-in-progress: true

jobs:
  dependency-review:
    name: Review Dependencies
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Dependency Review
        uses: actions/dependency-review-action@v5
        with:
          fail-on-severity: moderate
          deny-licenses: GPL-2.0, GPL-3.0
          # jszip is dual-licensed "(MIT OR GPL-3.0-or-later)", so a consumer is
          # free to take it under MIT and it is not a GPL dependency here. The
          # action matches the literal SPDX expression rather than evaluating
          # the OR, so the deny rule above trips on it. Allow it explicitly.
          # This is not new to any particular version - 3.10.1 carries the same
          # expression; it only surfaces now because dependency-review inspects
          # changed dependencies, and jszip had not changed before.
          allow-dependencies-licenses: pkg:npm/jszip
          comment-summary-in-pr: on-failure

  audit:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup Node.js 22 LTS
        uses: actions/setup-node@v7
        with:
          node-version: '22'

      - name: Run npm audit
        run: |
          npm audit --audit-level=moderate || true
          npm audit --audit-level=moderate --json > audit-results.json || true

      - name: Comment audit results
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          if [ -f audit-results.json ]; then
            VULNERABILITIES=$(jq '.metadata.vulnerabilities' audit-results.json)
            TOTAL=$(jq '.metadata.vulnerabilities | to_entries | map(.value) | add' audit-results.json)

            if [ "$TOTAL" -gt 0 ]; then
              echo "## Security Audit Results" > comment.md
              echo "" >> comment.md
              echo "Found $TOTAL vulnerabilities:" >> comment.md
              echo "" >> comment.md
              jq -r '.metadata.vulnerabilities | to_entries | .[] | "- **\(.key | ascii_upcase)**: \(.value)"' audit-results.json >> comment.md
              echo "" >> comment.md
              echo "Run \`npm audit fix\` to resolve fixable issues." >> comment.md

              gh pr comment "$PR_NUMBER" --body-file comment.md
            fi
          fi
